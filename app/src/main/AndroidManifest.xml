<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vn.asiantech.internship.footballmanager">

    <uses-permission android:name="android.permission.CAMERA"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />


    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/ic_football"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity_">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.league.LeagueActivity_" />
        <activity android:name=".ui.league.footballteam.FootBallTeamActivity_" />
        <activity android:name=".ui.league.player.PlayerActivity_" />
        <activity android:name=".ui.league.playerdetail.PlayDetailActivity_" />

        <meta-data
            android:name="DATABASE"
            android:value="football.db" />
        <meta-data
            android:name="VERSION"
            android:value="1" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="vn.asiantech.internship.footballmanager" />
    </application>

</manifest>
